<div class="page-header">
  <h1>Ranking</h1>
</div>
<table class="table table-striped">
  <thead>
  <tr>
    <th>Score</th>
    <th>User</th>
    <th>Username</th>
    <th>Time Ago</th>
  </tr>
  </thead>
  <tbody>
  <% @rankings.each do |user| %>
    <tr>
      <td><%= user.best_game.score %></td>
      <td><%= user.fullname %></td>
      <td><%= user.username %></td>
      <td><%= time_ago_in_words(user.best_game.created_at) %></td>
    </tr>
  <% end %>
  </tbody>
</table>
